import { Signature } from '@cedra-labs/ts-sdk';
import { UserResponse } from '../misc.mjs';

/** Version of the feature. */
type CedraSignMessageVersion = '1.0.0';
/** Name of the feature. */
declare const CedraSignMessageNamespace = "cedra:signMessage";
type CedraSignMessageFeature = {
    /** Namespace for the feature. */
    [CedraSignMessageNamespace]: {
        /** Version of the feature API. */
        version: CedraSignMessageVersion;
        signMessage: CedraSignMessageMethod;
    };
};
type CedraSignMessageMethod = (input: CedraSignMessageInput) => Promise<UserResponse<CedraSignMessageOutput>>;
type CedraSignMessageInput = {
    address?: boolean;
    application?: boolean;
    chainId?: boolean;
    message: string;
    nonce: string;
};
type CedraSignMessageOutput = {
    address?: string;
    application?: string;
    chainId?: number;
    fullMessage: string;
    message: string;
    nonce: string;
    prefix: 'CEDRA';
    signature: Signature;
};

export { type CedraSignMessageFeature, type CedraSignMessageInput, type CedraSignMessageMethod, CedraSignMessageNamespace, type CedraSignMessageOutput, type CedraSignMessageVersion };
