import { AccountAuthenticator, AnyRawTransaction, HexInput, Network } from "@cedra-labs/ts-sdk";
import { AccountInfo, CedraSignAndSubmitTransactionInput, CedraSignAndSubmitTransactionOutput, CedraSignInInput, CedraSignInOutput, CedraSignMessageInput, CedraSignMessageOutput, CedraSignTransactionInputV1_1, CedraSignTransactionOutputV1_1, NetworkInfo, UserResponse } from "@cedra-labs/wallet-standard";
import { ACDappClientConfig } from "@cedra-labs/dapp-sdk";
import { CedraConnectAccount } from "./CedraConnectAccount";
type WithSSOProvider<T> = T & {
    provider: "google" | "apple";
};
export interface CedraConnectWalletConfig extends Omit<ACDappClientConfig, "defaultNetworkName" | "provider"> {
    claimSecretKey?: HexInput;
    network?: Network;
    preferredWalletName?: string;
}
export declare abstract class CedraConnectWallet {
    protected static connectedAccountStorageKey: string;
    protected static get connectedAccount(): AccountInfo | undefined;
    protected static set connectedAccount(value: AccountInfo | undefined);
    readonly version = "1.0.0";
    readonly chains: readonly ["cedra:devnet", "cedra:testnet", "cedra:localnet", "cedra:mainnet"];
    get accounts(): CedraConnectAccount[];
    private readonly cedraClient;
    private readonly client;
    private readonly preferredWalletName?;
    private readonly claimOptions?;
    constructor({ claimSecretKey, network, preferredWalletName, ...clientConfig }: WithSSOProvider<CedraConnectWalletConfig>);
    connect(): Promise<UserResponse<AccountInfo>>;
    disconnect(): Promise<void>;
    signIn(input: CedraSignInInput): Promise<UserResponse<CedraSignInOutput>>;
    getAccount(): Promise<AccountInfo>;
    getNetwork(): Promise<NetworkInfo>;
    signMessage(input: CedraSignMessageInput): Promise<UserResponse<CedraSignMessageOutput>>;
    signTransaction(rawTxn: AnyRawTransaction): Promise<UserResponse<AccountAuthenticator>>;
    signTransaction(args: CedraSignTransactionInputV1_1): Promise<UserResponse<CedraSignTransactionOutputV1_1>>;
    signAndSubmitTransaction(args: CedraSignAndSubmitTransactionInput): Promise<UserResponse<CedraSignAndSubmitTransactionOutput>>;
    onAccountChange(_callback?: (newAccount: AccountInfo) => void): Promise<void>;
    onNetworkChange(_callback?: (newNetwork: NetworkInfo) => void): Promise<void>;
}
export {};
//# sourceMappingURL=CedraConnectWallet.d.ts.map