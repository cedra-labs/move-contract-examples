var __typeError = (msg) => {
  throw TypeError(msg);
};
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateAdd = (obj, member, value) => member.has(obj) ? __typeError("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);

// src/index.ts
import { registerWallet } from "@cedra-labs/wallet-standard";

// src/CedraConnectGoogleWallet.ts
import {
  CedraConnectNamespace,
  CedraDisconnectNamespace,
  CedraGetAccountNamespace,
  CedraGetNetworkNamespace,
  CedraOnAccountChangeNamespace,
  CedraOnNetworkChangeNamespace,
  CedraSignAndSubmitTransactionNamespace,
  CedraSignInNamespace,
  CedraSignMessageNamespace,
  CedraSignTransactionNamespace
} from "@cedra-labs/wallet-standard";

// src/CedraConnectWallet.ts
import {
  AccountAddress as AccountAddress2,
  Cedra,
  CedraConfig,
  Ed25519PrivateKey,
  MultiAgentTransaction,
  Network as Network2,
  NetworkToNodeAPI,
  SimpleTransaction
} from "@cedra-labs/ts-sdk";
import {
  AccountInfo as AccountInfo3,
  CEDRA_CHAINS as CEDRA_CHAINS2,
  CedraWalletError,
  CedraWalletErrorCode,
  UserResponseStatus as UserResponseStatus2
} from "@cedra-labs/wallet-standard";
import {
  deserializePublicKeyB64,
  serializePublicKeyB64
} from "@cedra-labs/crypto";
import { ACDappClient } from "@cedra-labs/dapp-sdk";

// src/CedraConnectAccount.ts
import {
  AnyPublicKey,
  Ed25519PublicKey,
  MultiEd25519PublicKey,
  MultiKey,
  SigningScheme
} from "@cedra-labs/ts-sdk";
import {
  CEDRA_CHAINS
} from "@cedra-labs/wallet-standard";
var _address, _publicKey;
var CedraConnectAccount = class {
  constructor({ address, ansName, publicKey }) {
    // region CedraWalletAccount
    this.chains = CEDRA_CHAINS;
    this.features = [];
    // endregion
    // region PetraAccount
    __privateAdd(this, _address);
    __privateAdd(this, _publicKey);
    __privateSet(this, _publicKey, publicKey);
    __privateSet(this, _address, address);
    this.label = ansName;
  }
  get address() {
    return __privateGet(this, _address).toString();
  }
  get publicKey() {
    return __privateGet(this, _publicKey).toUint8Array();
  }
  get signingScheme() {
    if (__privateGet(this, _publicKey) instanceof Ed25519PublicKey) {
      return SigningScheme.Ed25519;
    }
    if (__privateGet(this, _publicKey) instanceof MultiEd25519PublicKey) {
      return SigningScheme.MultiEd25519;
    }
    if (__privateGet(this, _publicKey) instanceof AnyPublicKey) {
      return SigningScheme.SingleKey;
    }
    if (__privateGet(this, _publicKey) instanceof MultiKey) {
      return SigningScheme.MultiKey;
    }
    throw new Error("Unsupported public key type");
  }
  // endregion
};
_address = new WeakMap();
_publicKey = new WeakMap();

// src/helpers.ts
import { Network, NetworkToChainId } from "@cedra-labs/ts-sdk";
import {
  AccountInfo as AccountInfo2,
  UserResponseStatus
} from "@cedra-labs/wallet-standard";
function customAccountToStandardAccount({
  address,
  name,
  publicKey
}) {
  return new AccountInfo2({
    address,
    ansName: name,
    publicKey
  });
}
function unwrapUserResponse(response, callback) {
  if (response.status === "dismissed") {
    return { status: UserResponseStatus.REJECTED };
  }
  return { args: callback(response.args), status: UserResponseStatus.APPROVED };
}
function networkToChainId(network) {
  switch (network) {
    case Network.MAINNET:
      return NetworkToChainId.mainnet;
    case Network.TESTNET:
      return NetworkToChainId.testnet;
    default:
      return NetworkToChainId.devnet;
  }
}

// src/CedraConnectWallet.ts
var _CedraConnectWallet = class _CedraConnectWallet {
  constructor({
    claimSecretKey,
    network = Network2.MAINNET,
    preferredWalletName,
    ...clientConfig
  }) {
    // endregion
    // region CedraWallet
    this.version = "1.0.0";
    this.chains = CEDRA_CHAINS2;
    this.client = new ACDappClient(clientConfig);
    if (!NetworkToNodeAPI[network]) {
      throw new Error("Network not supported");
    }
    const cedraConfig = new CedraConfig({ network });
    this.cedraClient = new Cedra(cedraConfig);
    this.preferredWalletName = preferredWalletName;
    this.claimOptions = claimSecretKey ? { network, secretKey: new Ed25519PrivateKey(claimSecretKey) } : void 0;
  }
  static get connectedAccount() {
    const serialized = localStorage.getItem(
      _CedraConnectWallet.connectedAccountStorageKey
    );
    if (!serialized) {
      return void 0;
    }
    try {
      const { address, publicKey } = JSON.parse(
        serialized
      );
      return new AccountInfo3({
        address: AccountAddress2.from(address),
        publicKey: deserializePublicKeyB64(publicKey)
      });
    } catch (err) {
      console.warn("Inconsistent state, resetting it");
      this.connectedAccount = void 0;
      return void 0;
    }
  }
  static set connectedAccount(value) {
    if (value !== void 0) {
      const serialized = {
        address: value.address.toString(),
        publicKey: serializePublicKeyB64(value.publicKey)
      };
      localStorage.setItem(
        _CedraConnectWallet.connectedAccountStorageKey,
        JSON.stringify(serialized)
      );
    } else {
      localStorage.removeItem(_CedraConnectWallet.connectedAccountStorageKey);
    }
  }
  // eslint-disable-next-line class-methods-use-this
  get accounts() {
    const { connectedAccount } = _CedraConnectWallet;
    return connectedAccount ? [new CedraConnectAccount(connectedAccount)] : [];
  }
  async connect() {
    const { connectedAccount } = _CedraConnectWallet;
    if (connectedAccount !== void 0) {
      return { args: connectedAccount, status: UserResponseStatus2.APPROVED };
    }
    const response = await this.client.connect({
      claimOptions: this.claimOptions,
      preferredWalletName: this.preferredWalletName
    });
    if (response.status === "dismissed") {
      return { status: UserResponseStatus2.REJECTED };
    }
    const newConnectedAccount = customAccountToStandardAccount(
      response.args.account
    );
    _CedraConnectWallet.connectedAccount = newConnectedAccount;
    return {
      args: newConnectedAccount,
      status: UserResponseStatus2.APPROVED
    };
  }
  async disconnect() {
    const { connectedAccount } = _CedraConnectWallet;
    if (connectedAccount) {
      await this.client.disconnect(connectedAccount.address);
      _CedraConnectWallet.connectedAccount = void 0;
    }
  }
  async signIn(input) {
    const response = await this.client.signIn({
      network: this.cedraClient.config.network,
      ...input
    });
    if (response.status === "dismissed") {
      return { status: UserResponseStatus2.REJECTED };
    }
    const output = response.args;
    _CedraConnectWallet.connectedAccount = output.account;
    return { args: output, status: UserResponseStatus2.APPROVED };
  }
  // eslint-disable-next-line class-methods-use-this
  async getAccount() {
    const { connectedAccount } = _CedraConnectWallet;
    if (!connectedAccount) {
      throw new CedraWalletError(CedraWalletErrorCode.Unauthorized);
    }
    return customAccountToStandardAccount(connectedAccount);
  }
  async getNetwork() {
    const { network } = this.cedraClient.config;
    const chainId = await this.cedraClient.getChainId();
    const url = NetworkToNodeAPI[network];
    return {
      chainId,
      name: network,
      url
    };
  }
  async signMessage(input) {
    const { connectedAccount } = _CedraConnectWallet;
    if (!connectedAccount) {
      throw new CedraWalletError(CedraWalletErrorCode.Unauthorized);
    }
    const chainId = networkToChainId(this.cedraClient.config.network);
    const { message, nonce } = input;
    const encoder = new TextEncoder();
    const messageBytes = encoder.encode(message);
    const nonceBytes = encoder.encode(nonce);
    const response = await this.client.signMessage({
      chainId,
      message: messageBytes,
      nonce: nonceBytes,
      signerAddress: connectedAccount.address
    });
    if (response.status === "dismissed") {
      return { status: UserResponseStatus2.REJECTED };
    }
    const { fullMessage, signature } = response.args;
    const extraResponseArgs = {
      address: connectedAccount.address.toString(),
      application: this.client.dappInfo.domain,
      chainId,
      message,
      nonce,
      prefix: "CEDRA"
    };
    return {
      args: {
        fullMessage,
        signature,
        ...extraResponseArgs
      },
      status: UserResponseStatus2.APPROVED
    };
  }
  async signTransaction(txnOrArgs, _asFeePayer) {
    const { connectedAccount } = _CedraConnectWallet;
    if (!connectedAccount) {
      throw new CedraWalletError(CedraWalletErrorCode.Unauthorized);
    }
    if ("bcsToBytes" in txnOrArgs) {
      const transaction = txnOrArgs;
      const feePayer = transaction.feePayerAddress ? { address: transaction.feePayerAddress } : void 0;
      const secondarySigners = transaction.secondarySignerAddresses?.map(
        (address) => ({ address })
      );
      const response2 = await this.client.signTransaction({
        feePayer,
        secondarySigners,
        signerAddress: connectedAccount.address,
        transaction: transaction.rawTransaction
      });
      return unwrapUserResponse(response2, (args) => args.authenticator);
    }
    const requestArgs = txnOrArgs;
    const response = await this.client.signTransaction({
      ...requestArgs,
      signerAddress: connectedAccount.address
    });
    return unwrapUserResponse(response, (responseArgs) => {
      const { authenticator, rawTransaction } = responseArgs;
      if (!rawTransaction) {
        throw new Error("Expected raw transaction in response args");
      }
      const secondarySigners = requestArgs.secondarySigners ?? [];
      let transaction;
      if (secondarySigners.length > 0) {
        transaction = new MultiAgentTransaction(
          rawTransaction,
          secondarySigners.map((s) => s.address),
          requestArgs.feePayer?.address
        );
      } else {
        transaction = new SimpleTransaction(
          rawTransaction,
          requestArgs.feePayer?.address
        );
      }
      return {
        authenticator,
        rawTransaction: transaction
      };
    });
  }
  async signAndSubmitTransaction(args) {
    const { gasUnitPrice, maxGasAmount, payload } = args;
    const { connectedAccount } = _CedraConnectWallet;
    if (!connectedAccount) {
      throw new CedraWalletError(CedraWalletErrorCode.Unauthorized);
    }
    const response = await this.client.signAndSubmitTransaction({
      gasUnitPrice,
      maxGasAmount,
      network: this.cedraClient.config.network,
      payload,
      signerAddress: connectedAccount.address
    });
    if (response.status === "dismissed") {
      return { status: UserResponseStatus2.REJECTED };
    }
    return {
      args: { hash: response.args.txnHash },
      status: UserResponseStatus2.APPROVED
    };
  }
  // eslint-disable-next-line class-methods-use-this
  async onAccountChange(_callback) {
  }
  // eslint-disable-next-line class-methods-use-this
  async onNetworkChange(_callback) {
  }
  // endregion
};
// region connectedAccount
_CedraConnectWallet.connectedAccountStorageKey = "@cedra-labs/connectedAccount";
var CedraConnectWallet = _CedraConnectWallet;

// src/config.ts
var walletMetadata = {
  apple: {
    icon: (
      // eslint-disable-next-line max-len
      "data:image/svg+xml;base64,"
    ),
    name: "Continue with Apple",
    url: "https://cedraconnect.app"
  },
  google: {
    icon: (
      // eslint-disable-next-line max-len
      "data:image/svg+xml;base64,"
    ),
    name: "Continue with Google",
    url: "https://cedraconnect.app"
  }
};

// src/CedraConnectGoogleWallet.ts
var CedraConnectGoogleWallet = class extends CedraConnectWallet {
  constructor(config) {
    super({ ...config, provider: "google" });
    this.name = walletMetadata.google.name;
    this.icon = walletMetadata.google.icon;
    this.url = walletMetadata.google.url;
  }
  // region CedraWallet
  get features() {
    return {
      [CedraConnectNamespace]: {
        connect: this.connect.bind(this),
        version: "1.0.0"
      },
      [CedraDisconnectNamespace]: {
        disconnect: this.disconnect.bind(this),
        version: "1.0.0"
      },
      [CedraGetAccountNamespace]: {
        account: this.getAccount.bind(this),
        version: "1.0.0"
      },
      [CedraGetNetworkNamespace]: {
        network: this.getNetwork.bind(this),
        version: "1.0.0"
      },
      [CedraOnAccountChangeNamespace]: {
        onAccountChange: this.onAccountChange.bind(this),
        version: "1.0.0"
      },
      [CedraOnNetworkChangeNamespace]: {
        onNetworkChange: this.onNetworkChange.bind(this),
        version: "1.0.0"
      },
      [CedraSignAndSubmitTransactionNamespace]: {
        signAndSubmitTransaction: this.signAndSubmitTransaction.bind(this),
        version: "1.1.0"
      },
      [CedraSignInNamespace]: {
        signIn: this.signIn.bind(this),
        version: "0.1.0"
      },
      [CedraSignMessageNamespace]: {
        signMessage: this.signMessage.bind(this),
        version: "1.0.0"
      },
      [CedraSignTransactionNamespace]: {
        signTransaction: this.signTransaction.bind(this),
        version: "1.1"
      }
    };
  }
  // endregion
};

// src/CedraConnectAppleWallet.ts
import {
  CedraConnectNamespace as CedraConnectNamespace2,
  CedraDisconnectNamespace as CedraDisconnectNamespace2,
  CedraGetAccountNamespace as CedraGetAccountNamespace2,
  CedraGetNetworkNamespace as CedraGetNetworkNamespace2,
  CedraOnAccountChangeNamespace as CedraOnAccountChangeNamespace2,
  CedraOnNetworkChangeNamespace as CedraOnNetworkChangeNamespace2,
  CedraSignAndSubmitTransactionNamespace as CedraSignAndSubmitTransactionNamespace2,
  CedraSignInNamespace as CedraSignInNamespace2,
  CedraSignMessageNamespace as CedraSignMessageNamespace2,
  CedraSignTransactionNamespace as CedraSignTransactionNamespace2
} from "@cedra-labs/wallet-standard";
var CedraConnectAppleWallet = class extends CedraConnectWallet {
  constructor(config) {
    super({ ...config, provider: "apple" });
    this.name = walletMetadata.apple.name;
    this.icon = walletMetadata.apple.icon;
    this.url = walletMetadata.apple.url;
  }
  // region CedraWallet
  get features() {
    return {
      [CedraConnectNamespace2]: {
        connect: this.connect.bind(this),
        version: "1.0.0"
      },
      [CedraDisconnectNamespace2]: {
        disconnect: this.disconnect.bind(this),
        version: "1.0.0"
      },
      [CedraGetAccountNamespace2]: {
        account: this.getAccount.bind(this),
        version: "1.0.0"
      },
      [CedraGetNetworkNamespace2]: {
        network: this.getNetwork.bind(this),
        version: "1.0.0"
      },
      [CedraOnAccountChangeNamespace2]: {
        onAccountChange: this.onAccountChange.bind(this),
        version: "1.0.0"
      },
      [CedraOnNetworkChangeNamespace2]: {
        onNetworkChange: this.onNetworkChange.bind(this),
        version: "1.0.0"
      },
      [CedraSignAndSubmitTransactionNamespace2]: {
        signAndSubmitTransaction: this.signAndSubmitTransaction.bind(this),
        version: "1.1.0"
      },
      [CedraSignInNamespace2]: {
        signIn: this.signIn.bind(this),
        version: "0.1.0"
      },
      [CedraSignMessageNamespace2]: {
        signMessage: this.signMessage.bind(this),
        version: "1.0.0"
      },
      [CedraSignTransactionNamespace2]: {
        signTransaction: this.signTransaction.bind(this),
        version: "1.1"
      }
    };
  }
  // endregion
};

// src/index.ts
function registerCedraConnect(config = {}) {
  const googleWalletPlugin = new CedraConnectGoogleWallet(config);
  const appleWalletPlugin = new CedraConnectAppleWallet(config);
  registerWallet(googleWalletPlugin);
  registerWallet(appleWalletPlugin);
}
export {
  CedraConnectAccount,
  CedraConnectAppleWallet,
  CedraConnectGoogleWallet,
  CedraConnectWallet,
  registerCedraConnect
};
//# sourceMappingURL=index.mjs.map